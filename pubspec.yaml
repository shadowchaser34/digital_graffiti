name: digital_graffiti_wall
description: A collaborative digital graffiti wall (university project).
publish_to: "none"
version: 0.1.0
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  cupertino_icons: ^1.0.2
  camera: ^0.10.5+9
  permission_handler: ^12.0.1
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true